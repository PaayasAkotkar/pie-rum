// Package pierum...
package pierum

// rum-sdk: provides the build in-feature allowing the developers to create their own layer and toggle system via api calls
// can use the cli for metadata, config

// store can have many profiles
// profiles can have many services
// services can have many dispatchers
// dispatchers can have many events

// the add and set acts as auto ranking methods
// where add is auto assigning the ranking and set is explicitaly defining it

// v1 features:
// 1. toggle system
// 2. bake and layer

// from server side you implement all
// from client side you simply push the name of the profile or profiles to trigger
